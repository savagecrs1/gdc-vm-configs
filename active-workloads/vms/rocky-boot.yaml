apiVersion: vm.cluster.gke.io/v1
kind: VirtualMachineDisk
metadata:
  name: rocky-boot
  namespace: vms
spec:
  size: 20Gi
  storageClassName: local-shared
  source:
    gcs:
      url: gs://chris-vm-images-1777379240/Rocky-10-GenericCloud-Base.latest.x86_64.qcow2
      serviceAccount: ksa-vm-puller

